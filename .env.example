# Copy this file to .env.local and fill in real keys.
# .env.local is git-ignored.

# Required for POST /api/transcribe (OpenAI Whisper).
OPENAI_API_KEY=sk-...

# Required for POST /api/interview (Claude).
ANTHROPIC_API_KEY=sk-ant-...
